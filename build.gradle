group 'by.botyanov.wallet'
version '1.0-SNAPSHOT'

subprojects {
    ext {
        grpcStarterVersion = '3.2.2'
    }

    repositories {
        mavenCentral()
    }
    group 'by.botyanov.wallet'

    apply plugin: 'java'
    sourceCompatibility = '11'
    targetCompatibility = '11'
}
